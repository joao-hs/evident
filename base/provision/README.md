# Provisioning utility

This directory will be copied to the guest VM's `/home/$USER/provision`.
It contains:
- initial scripts to configure the VM automatically upon successful installation
- scripts/utilities to assist the provisioning of workloads
- closing scripts to make the VM ready to be shipped
- some secrets to allow non-interactive provisioning

### Details

TO-DO
